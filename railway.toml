[build]
buildCommand = "npm install --prefix nuctributario"

[deploy]
startCommand = "cd nuctributario && node server.js"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
